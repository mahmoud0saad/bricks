import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppRadius {
  static BorderRadius radiusExtraSmall = BorderRadius.circular(5.r);
  // static  BorderRadius radiusSmall = BorderRadius.circular(8); //enable if you used
  static BorderRadius radiusMedium = BorderRadius.circular(10.r);
  static  BorderRadius radiusLarge = BorderRadius.circular(15.r); //enable if you used
  static  BorderRadius radiusLargeToVertical = BorderRadius.vertical(top:Radius.circular(15.r)); //enable if you used
  static BorderRadius radiusExtraLarge = BorderRadius.circular(20.r);
  static BorderRadius radiusExtraLargeToVertical = BorderRadius.vertical(top:Radius.circular(20.r));
}
